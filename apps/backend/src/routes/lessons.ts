import { Router } from 'express'
// import { requireAuth } from '../middleware/auth'

const router = Router()

// TODO: Build out the lessons routes
// Refer to the build plan in server.ts for guidance

export default router
