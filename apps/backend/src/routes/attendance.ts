import { Router } from 'express'
// import { requireAuth } from '../middleware/auth'

const router = Router()

// TODO: Build out the attendance routes
// Refer to the build plan in server.ts for guidance

export default router
